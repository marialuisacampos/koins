{
  "name": "koins-monorepo",
  "version": "1.0.0",
  "description": "Koins - Equilíbrio financeiro a dois",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:pwa\" \"npm run dev:backend\"",
    "dev:pwa": "cd pwa && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:pwa && npm run build:backend",
    "build:pwa": "cd pwa && npm run build",
    "build:backend": "cd backend && npm run build",
    "install:all": "npm install && cd pwa && npm install && cd ../backend && npm install",
    "lint": "cd pwa && npm run lint && cd ../backend && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

